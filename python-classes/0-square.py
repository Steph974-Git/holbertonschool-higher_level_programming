#!/usr/bin/python3
"""
Module définissant une classe Square (carré) vide.

En POO (Programmation Orientée Objet), un module est simplement
un fichier Python qui peut contenir des classes, fonctions et variables.
"""


class Square:
    """
    Classe vide représentant un carré.

    En POO, une classe est un plan ou un modèle qui définit:
    - Les attributs (données/propriétés que les objets peuvent stocker)
    - Les méthodes (fonctions/comportements que les objets peuvent exécuter)

    Une instance est un objet spécifique créé à partir d'une classe.
    Exemples:
        carre1 = Square()  # Une première instance de Square
        carre2 = Square()  # Une deuxième instance différente

    Cette classe est actuellement vide (aucun attribut ni méthode).
    """
    pass  # L'instruction 'pass' indique un bloc de code vide intentionnel
    # Elle permet de définir une classe sans contenu pour l'instant
